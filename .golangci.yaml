version: "2"
run:
  timeout: 5m
  # Search for config up to the root
  allow-parallel-runners: true

linters:
  enable:
    - govet
    - gosec
    - goconst
    - unused
    - revive
    - staticcheck
    - errcheck
    - gosimple
    - gocritic
    - ineffassign
    - dupl
    - typecheck
